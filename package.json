{
  "name": "bluish",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*",
    "plugins/*"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "test": "rimraf ./{packages,plugins}/**/dist && jest .",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@bluish/eslint-config": "^0.0.5",
    "@commitlint/cli": "^17.3.0",
    "@commitlint/config-conventional": "^17.3.0",
    "@types/jest": "^29.2.2",
    "eslint": "^8.28.0",
    "husky": "^8.0.2",
    "jest": "^29.3.1",
    "lerna": "^6.0.3",
    "rimraf": "^3.0.2",
    "ts-jest": "^29.0.3",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.8.4"
  }
}
